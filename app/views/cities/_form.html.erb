<%= form_with(model: city, local: true) do |form| %>
  <% if city.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(city.errors.count, "error") %> prohibited this city from being saved:</h2>

      <ul>
      <% city.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :localName %>
    <%= form.text_field :localName %>
  </div>

  <div class="form-group">
  <%= form.label :region_id %>
  <%= form.select :region_id, options_for_select(region_pairs) %>
  </div>

  <div class="form-group">
  <%= form.label :intro %>
  <%= form.text_area  :intro, autofocus: true, autocomplete: "Intro", 
  class: 'form-control' %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :content %>
    <%= form.cktext_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
